export interface Componente {
    icon: string;
    name: string;
    routerLink: string;
    id: string;
}

export interface PuntosR {
    titulo: string,
    subtitle: string,
    title: string,
    desc: string,
    img: string
}